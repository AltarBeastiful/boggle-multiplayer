node_modules/
dist/
*.tsbuildinfo
.env
.DS_Store

.work/
audit-mobile/

# Runtime state: the daily grid results. Kept on a volume in production.
server/.state/

# Bundled definitions: 7 MB of gzip, which never delta-compresses, so every
# rebuild left a permanent copy in history. Downloaded from the latest release
# by scripts/deploy.sh, or rebuilt with scripts/build-definitions.mjs.
server/data/definitions.tsv.gz

# The words nothing defines: the leftovers of the same build, written out so
# the gap is a list somebody can read. Published with the release.
server/data/words-without-definition.txt
